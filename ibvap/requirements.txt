# ============================================================
# IBVAP - Pinned dependencies (Step 1)
# Install with: pip install -r requirements.txt
# ============================================================

# --- Core web/service framework (used by all 3 teams) ---
fastapi==0.115.0
uvicorn[standard]==0.30.6
pydantic==2.9.2
websockets==13.1

# --- Database ---
psycopg2-binary==2.9.9
asyncpg==0.29.0
SQLAlchemy==2.0.35

# --- Computer vision / perception team ---
opencv-python>=4.10.0
ultralytics>=8.3.10
easyocr>=1.7.2
mediapipe>=0.10.14
torch>=2.4.1
torchvision>=0.19.1
torchreid>=0.2.5
insightface>=0.7.3

# --- Re-ID / vector search (reasoning team) ---
faiss-cpu>=1.9.0
numpy>=1.26.4,<2.0.0

# --- Security (shared) ---
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4

# --- Blockchain client (platform team - adjust once Fabric SDK/network is chosen) ---
# hfc==0.9.0   # Hyperledger Fabric Python SDK - uncomment once network topology (Step 3) is finalized

# --- Testing (all teams) ---
pytest==8.3.3
pytest-asyncio==0.24.0
httpx==0.27.2

# --- Dev tooling ---
python-dotenv==1.0.1